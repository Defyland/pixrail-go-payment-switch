# Benchmark Results Status

The repository includes a measured local native benchmark baseline and k6 scripts for external load profiles. k6 results should be regenerated when persistent adapters are added because PostgreSQL, Redis, and broker delivery will change latency shape.
